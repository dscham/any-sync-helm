apiVersion: v1
kind: Secret
metadata:
  name: {{ include "any-sync.fullname" . }}-minio-credentials
  labels:
    {{- include "any-sync.labels" . | nindent 4 }}
type: Opaque
stringData:
  AWS_ACCESS_KEY_ID: {{ .Values.minio.auth.rootUser | quote }}
  AWS_SECRET_ACCESS_KEY: {{ .Values.minio.auth.rootPassword | quote }}
  credentials: |
    [default]
    aws_access_key_id={{ .Values.minio.auth.rootUser }}
    aws_secret_access_key={{ .Values.minio.auth.rootPassword }}
