apiVersion: apps/v1
kind: Deployment
metadata:
  labels:
    app.kubernetes.io/name: any-sync-filenode
    app.kubernetes.io/instance: '{{ .Release.Name }}'
    app.kubernetes.io/managed-by: '{{ .Release.Service }}'
  name: any-sync-filenode
spec:
  replicas: 1
  selector:
    matchLabels:
      app.kubernetes.io/name: any-sync-filenode
      app.kubernetes.io/instance: '{{ .Release.Name }}'
  strategy:
    type: Recreate
  template:
    metadata:
      labels:
        app.kubernetes.io/name: any-sync-filenode
        app.kubernetes.io/instance: '{{ .Release.Name }}'
        app.kubernetes.io/managed-by: '{{ .Release.Service }}'
    spec:
      containers:
      - image: '{{ .Values.filenode.image.repository }}:{{ .Values.filenode.image.tag }}'
        name: any-sync-filenode
        ports:
        - containerPort: '{{ .Values.filenode.port }}'
          protocol: TCP
        - containerPort: '{{ .Values.filenode.quicPort }}'
          protocol: UDP
        - containerPort: 8000
          protocol: TCP
        resources:
          limits:
            memory: '{{ .Values.filenode.resources.limits.memory }}'
        volumeMounts:
        - mountPath: /etc/any-sync-filenode
          name: any-sync-filenode-claim0
        - mountPath: /root/.aws
          name: any-sync-filenode-claim1
          readOnly: true
        - mountPath: /networkStore
          name: any-sync-filenode-claim2
      restartPolicy: Always
      volumes:
      - name: any-sync-filenode-claim0
        persistentVolumeClaim:
          claimName: any-sync-filenode-claim0
      - name: any-sync-filenode-claim1
        persistentVolumeClaim:
          claimName: any-sync-filenode-claim1
          readOnly: true
      - name: any-sync-filenode-claim2
        persistentVolumeClaim:
          claimName: any-sync-filenode-claim2
