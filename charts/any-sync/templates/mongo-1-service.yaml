apiVersion: v1
kind: Service
metadata:
  labels:
    app.kubernetes.io/name: mongo-1
    app.kubernetes.io/instance: '{{ .Release.Name }}'
    app.kubernetes.io/managed-by: '{{ .Release.Service }}'
  name: mongo-1
spec:
  ports:
  - name: mongo-tcp
    port: {{ .Values.mongo.port }}
    targetPort: {{ .Values.mongo.port }}
  selector:
    app.kubernetes.io/name: mongo-1
    app.kubernetes.io/instance: '{{ .Release.Name }}'
  clusterIP: None
