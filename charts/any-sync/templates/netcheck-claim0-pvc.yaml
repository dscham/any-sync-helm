apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  labels:
    io.kompose.service: netcheck-claim0
  name: netcheck-claim0
spec:
  accessModes:
  - ReadWriteOnce
  resources:
    requests:
      storage: 100Mi
